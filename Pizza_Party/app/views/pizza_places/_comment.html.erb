<div>
  <h4>Comment by <%= comment.user_id %></h4>
  <p>Posted <%= distance_of_time_in_words(comment.pizza_place.created_at, comment.created_at) %> ago</p>
  <p class="comment"><%= comment.body %></p>
</div>
