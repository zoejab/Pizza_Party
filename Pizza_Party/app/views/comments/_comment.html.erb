<div>

  <p class="timestamp"><%= time_ago_in_words(comment.created_at) %>
     ago</p>

  <p class="comment"><%= comment.body %></p>


<!-- <%#= link_to "Delete", pizza_place_comment_path(comment.id), method: :delete, data: {confirm: "Really delete the comment?"} %><br> -->
<!--
<%#= link_to "Edit", edit_pizza_place_comment_path(@pizza_place.id, comment.id) %> -->

<!-- <%#= link_to "Edit", edit_pizza_place_comment_path(:id, :pizza_place_id) %> -->

  <%= button_to 'DELETE', pizza_place_comment_path(comment.pizza_place_id, comment.id), :method => :delete %>

  <%= button_to 'EDIT', edit_pizza_place_comment_path(comment.pizza_place_id, comment.id) %>

</div>
